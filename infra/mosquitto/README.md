# Broker MQTT (Mosquitto)

Contiene la configuración y datos persistentes del broker MQTT usado por el nodo Edge.

- `mosquitto.conf` → configuración principal del servicio.
- `data/` → volumen para almacenamiento persistente (no versionado en Git).
